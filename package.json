{
  "name": "press-release-csv-file-parser",
  "version": "0.1.0",
  "description": "CSV parser microservice that has an endpoint which finds a CSV file and returns the rows of the CSV as JSON",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael <michael.vervloet@ordina.be> (https://www.ordina.be)",
  "license": "MIT",
  "dependencies": {
    "neat-csv": "^6.0.1"
  }
}
